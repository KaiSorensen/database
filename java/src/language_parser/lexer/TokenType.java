package language_parser.lexer;

public enum TokenType {
    KEYWORD,
    IDENTIFIER,
    STRING,
    INTEGER,
    BOOLEAN,
    LEFT_PAREN,
    RIGHT_PAREN,
    COMMA,
    DOT,
    SEMICOLON,
    EOF
}
